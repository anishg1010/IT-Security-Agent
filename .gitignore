# Generated artifacts — build output, not source. Everything here is
# regenerated by running the code, so it does not belong in version control.

# figures (the notebook renders inline; use `python visuals.py --save figures`
# only when you need images for a slide deck)
figures/
fig*.png
*.png

# trained model + generated dataset
match_model.joblib
match_dataset.json
confidence_model.joblib
confidence_training.json
eval_negatives.json

# python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# OS noise
.DS_Store
Thumbs.db

# NOTE: nvd_real_bulk.json IS committed on purpose — it is the input data the
# results depend on, so keeping it makes the work reproducible. If your course
# repo has a size limit, move it to a release/LFS and document the download.
